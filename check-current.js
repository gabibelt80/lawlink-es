const mysql = require("mysql2/promise"); (async () => { const conn = await mysql.createConnection({ host: "localhost", port: 3306, user: "juridictas", password: "juridictas" }); const [rows] = await conn.query("SELECT fu.id, fu.name, fu.email, fu.firmId, f.slug, f.name as firmName FROM juridictas.FirmUser fu LEFT JOIN juridictas.Firm f ON fu.firmId = f.id WHERE fu.email IN (?, ?)", ["egbeltrando@gmail.com", "gabi@juridictas.ar"]); console.log(JSON.stringify(rows, null, 2)); await conn.end(); })();
