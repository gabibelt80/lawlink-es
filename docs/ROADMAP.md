# 律师案件管理系统下一阶段产品路线图

> 版本：v1.0
> 日期：2026-07-04
> 基线：main @ 4f5af22 + 当前工作区（v0.47 程序工作台 / 短信附件提取 / 商事仲裁案由约束，未提交）
> 性质：产品路线图（该做什么、不该做什么），不是排期承诺。进入实施前按 AGENTS.md 惯例逐项写入 PRD「已锁定决策」。

---

## 一、判断依据

LawLink 从 v0.5 走到 v0.47，主线功能已齐：收案审批、多程序案件、程序工作台、财务开票、归档审批、法院短信解析、财产保全（三层模型）、文书模板卷宗、用章审批、AI 三件套（案由推荐/文书审查/类案检索）、报表。**广度不缺，缺的是深度和触达。**

三个信号支撑本路线图的取舍：

1. **导航史就是使用率史**。快递、工具、服务中心被移进顶栏「应用」角落（v0.37），保全一级入口被隐藏（v0.45），收件箱曾因「使用率低」被隐藏又在 v0.47 恢复。被边缘化过的功能不要再加码。
2. **代码体检结果**（2026-07-04，详见第四节）：新旧两套保全模型并存、Decimal 手工序列化在 6+ 处 action 重复出现、阶段与材料靠字符串标签关联——这些是继续叠功能前必须处理的地基问题。
3. **目标用户是独立律师和 1–5 人小团队**（PRD §一）。凡是「大所合规思维」的流程（多级审批、字段级权限、审核流），对这个用户群都是负资产。

---

## 二、下一阶段该做的模块（按优先级）

### P0 法定期限引擎 —— 最高价值，唯一「不做会出执业事故」的缺口

**现状**：`Deadline` 全靠手填，`remindDays` 手设。短信解析已能抽出「上诉期限 15 日内」这类文本（`sms-parser.ts` importantItems），但只是展示，不落库、不计算。

**为什么是 P0**：律师执业责任险理赔头号原因就是漏期限。案管系统能替律师挡住这件事，就值回全部使用成本；挡不住，其他功能做得再多也只是记事本。

**怎么做**：
1. 新表 `DeadlineRule`（规则库）：触发事件（裁判文书签收/受理通知送达/举证通知签收）→ 期限天数 → 期限类别 → 法条依据。内置规则 seed + 律所可改。
2. 触发点挂在已有动作上：程序录入「裁判签收日」→ 自动生成上诉期 Deadline（判决 15 日/裁定 10 日）；短信解析出举证期限 → 一键生成时带规则依据。
3. 每条自动生成的期限展示法条出处，可点开核对（已有元典 MCP 接入，可直接跳法条详情）。

**法律依据核验状态**（按工作区规则，写入代码前逐条过元典）：
- ✅ 已核验：上诉期——《民事诉讼法（2023 修正）》第 171 条，不服判决 15 日、不服裁定 10 日，现行有效（元典 2026-07-04 核验）。
- ⚠️ 未核验：保全续封 1/2/3 年期限的现行条文出处。**注意：PRD §13.3 写的「民诉法第 244 条」经查民诉法正文并无保全期限天数规定，该引用需要复核**；1/2/3 年的通说出处是最高法查扣冻规定（2020 修正），但具体条号本次未能通过元典检索确认。实施期限引擎时这是第一批要逐条核验的规则。

### P1 提醒触达出站 —— 站内提醒对不常开系统的人等于没有提醒

**现状**：通知只有站内 Popover + dashboard 卡片；cron 框架（`src/server/cron/scheduler.ts`）和到期扫描 job 已有，但提醒出不了系统。

**怎么做**（按运维成本从低到高，全部可选配置）：
1. **ICS 订阅源**：每用户一个带 token 的日历订阅 URL，开庭/期限/保全到期自动进手机日历。零第三方依赖，自部署友好，一天工作量。
2. **Webhook 推送**：企业微信/钉钉群机器人 URL 配置一条即可用，国内小所几乎都有微信群。
3. **SMTP 邮件**：可选，不强制配。

### P2 短信收件箱深化 —— 方向已对，把闭环走完

**现状**：本次未提交改动已加附件提取、凭证识别、重要事项抽取。缺的闭环：
1. **案号回填**：立案通知短信解析出新案号 → 反向回填 `MatterProcedure.caseNumber`（现在只用案号做匹配，没利用它补数据）。
2. **自动生成建议**：解析置信度高的开庭/期限，直接给「一键生成」预填草稿（时间、法庭、法官全带上），而不是让用户从头填。
3. **待人工处理队列**：`LOGIN_REQUIRED` 的电子送达现在埋在每条短信卡片里，应有跨短信的聚合视图（见第四节 parsedJson 问题）。

### P3 文书模板扩容 —— 基础设施已好，价值随模板数线性增长

docxtemplater 渲染链路、变量拼装、卷宗归档都已落地。要做的只是内容工作：预置模板从 8 个扩到 20+（PRD §11.8 既定计划），补律所自定义模板上传（v0.9 规划项）。不需要新架构。

### P4 移动端适配收尾

律师核对期限、看开庭安排的场景发生在法院走廊和路上。v0.10 做过一轮响应式，程序工作台（v0.47 新增的两栏布局）尚未适配窄屏。范围收紧为：工作台、案件详情、日程三个页面在手机上可读可点即可，不做原生 App（既定决策）。

---

## 三、伪需求清单（砍掉或冻结，不再投入）

| 功能 | 状态 | 判断 |
|---|---|---|
| **快递追踪** `ExpressTracking` | 已实现，nav 已边缘化 | **冻结**。依赖快递鸟/快递100 付费 API key，自部署小所不会去申请；寄快递的人手机上有物流通知。保留现有代码，不修不扩。 |
| **用章审批扩展**（自定义章种类、用章月报、统计图表，v0.9 规划） | 未做 | **砍掉规划**。5 人以下的所用章是「喊一声主任」的事，现有 5 种章 + 基础流程已经过度；再投入是大所合规思维。 |
| **CommissionPlan 自动分成** | 已实现 | **降级为只读记录**。独立律师无分成场景；小团队分成一案一议，自动生成 COMMISSION 条目反而制造对账噪音。不删数据，停止在 UI 主流程里推它。 |
| **外部联系人审核流** | 已实现 | **默认关闭**。小所是信任环境，加联系人要等审批只会让人放弃使用通讯录。改为设置项，默认直接通过。 |
| **客户门户 / SaaS 多租户**（V2 远景） | 规划中 | **从愿景里删除**。单体单库是当前全部权限模型的前提，为「未来可能的 SaaS」保留架构弹性会污染每一个权限判断。真有需求时另起项目。 |
| **主题/视觉继续迭代** | v0.6、v0.7、v3 已三轮重做 | **冻结一个版本周期**。视觉已到「不难看」的水位，边际收益趋零；每轮全站重做都产生数百文件 diff，挤占审阅带宽。 |
| **自建向量检索**（V2 远景「案件向量检索」） | 规划中 | **砍掉**。类案检索已接元典 `rh_ptal_search`，自建向量库是重复造轮子，还要自己养 embedding 基础设施。 |
| **工时统计/效率报表**（V2 远景） | 规划中 | **不做通用工时**。目标用户按件收费为主。若顾问单位对账有真实需求，做「顾问服务台账」这种轻量记录，不做计时器。 |

---

## 四、会拖后腿的数据结构（按危害排序）

### 1. 新旧两套保全模型并存 ⚠️ 最优先处理

`Preservation`/`PreservationRenewal`（旧单表）与 `PreservationCase→Target→Property→PropertyRenewal`（v0.44 三层）同时存在于 schema。**旧模型的 `src/server/preservations/actions.ts` 已无任何页面引用（2026-07-04 grep 确认），是死代码，但本次工作区 diff 还在给它加 Decimal 转换**——维护成本在向死代码泄漏。

**行动**：确认旧表无存量数据（或写一次性迁移脚本挪到新模型）→ 删除旧模型 + 旧 actions + dashboard/schedule 里的兼容读取。涉及 schema 迁移和删文件，属红线，需叶森批准后执行。

### 2. `SmsMessage.parsedJson` 单 JSON 字段膨胀

解析结果、AI 增强、附件提取操作日志（attachmentResults）全塞一个 JSON。后果已经显现：`normalizeStoredParsed()` 这种版本兼容 shim 出现了；「哪些电子送达待人工处理」这类查询无法用 SQL 表达，只能全量拉出来在内存里过滤。

**行动**：附件提取结果拆成 `SmsAttachment` 子表（url/status/documentId/checkedAt），或最低成本方案——`SmsMessage` 上加 `needsManualAction Boolean` 冗余列供查询。解析结果本体留在 JSON 没问题。

### 3. 阶段↔材料靠字符串标签关联

材料归集到环节用的是 `Document.tags` 里的 `"阶段:开庭审理"` 字符串；删除环节的保护逻辑靠 `stage.name.includes("保全")`。**环节一旦支持改名，材料链接全部断掉**；同名环节跨程序也无法区分。PRD §10.2.1 自己已承认需要环节状态字段。

**行动**：`Document.stageId` 外键（可空）+ `MatterStage.status`（ACTIVE/HIDDEN）。标签方案可保留作展示，但关联关系必须是外键。

### 4. Decimal 手工序列化在每个 action 重复

本次 diff 为修 RSC 序列化在 6+ 个 action 手写 `decimalToNumber` map（finance/intakes/matters/preservations×2/invoices）。**每新增一个带金额的查询都可能漏一处，漏了就是运行时报错**，typecheck 拦不住（`Prisma.Decimal` 有 `toString`，结构上兼容）。

**行动**：收口到一处——Prisma client extension 里对 Decimal 字段统一 `result` 转换，或建立 server action 出口的统一 serialize 层。把 `src/lib/decimal.ts` 从「到处调」变成「只在一处调」。

### 5. 案由联动校验三处重复

`assertCauseAllowedForSelection` 在 createIntake / convertIntakeToMatter / createMatter / updateMatterBasicInfo 各调一次，且 `updateMatterBasicInfo` 取的是「第一个程序」的类型——若首程序是补录的 INFORMATIONAL 前序程序（如别人代理的一审），校验基准就错了。规则再变（仲裁法还会修）要改多处。

**行动**：校验基准统一为「当前 ENGAGED 的首个程序」，并把调用收口到一个入口函数。

### 6. TimelineEvent / AuditLog 手工双写

每个 server action 靠自觉记 timeline + audit，漏写无感知（本次新增的 stage 增删记了，历史上有没有漏的没人知道）。中期在 service 层做统一记录入口；短期至少在 code review checklist 里列明。

---

## 五、应该自动化的流程

| 流程 | 现状 | 自动化方案 | 依赖 |
|---|---|---|---|
| **裁判签收 → 上诉期/履行期期限** | 手填 | 录入签收日自动生成级联 Deadline，附法条依据 | P0 期限引擎 |
| **保全到期 → 期限体系** | dashboard 卡片有显示，但未进 `Deadline`（PRD §13.3「派生条目」一直没做，日程里是查询时临时拼的） | 统一：到期前 30 天自动生成一条真实 Deadline，走统一提醒通道 | P0/P1 |
| **立案短信 → 案号回填** | 案号只用于匹配案件 | 解析出的新案号自动回填 `MatterProcedure.caseNumber`（带确认） | P2 |
| **短信 → 开庭/期限草稿** | 一键生成但字段要手填 | 高置信解析直接预填草稿待确认 | P2 |
| **数据库备份** | `scripts/backup.sh` 已有，靠手跑 | 接入已有 cron scheduler，备份成功/失败发通知；PRD §六承诺的「内置 pg_dump 备份」补完最后一公里 | 无 |
| **裁判结果 → 后续程序衔接** | 工作台 checklist 文字提醒 | 「新增执行/上诉程序」一键动作，预填前序程序信息 | 无 |
| **发票 OCR、归档超期扫描、案由推荐** | 已自动化 | 保持 | — |

---

## 六、分期建议

| 阶段 | 内容 | 判断标准 |
|---|---|---|
| **v0.48（地基）** | 第四节 1–4 项技术债：删旧保全模型（需批准）、parsedJson 拆表、stageId 外键、Decimal 序列化收口 | 先还债再叠功能；每项都是「现在 1 天，半年后 1 周」的债 |
| **v0.49（期限引擎）** | P0：DeadlineRule 规则库 + 触发点 + 法条核验（逐条过元典） | 上线后新建的诉讼程序，上诉期不再需要手填 |
| **v0.50（触达）** | P1：ICS 订阅 + webhook 推送；备份 cron 化 | 律师一周不打开系统也不会漏期限 |
| **v0.51（收件箱闭环）** | P2：案号回填、生成草稿、待人工队列 | 一条法院短信从粘贴到落库 ≤ 2 次点击 |
| **v1.0（收口发布）** | P3 模板扩容 + P4 移动端收尾 + 冻结项清理（外部联系人审核默认关、分成降级） | 对外可推荐给同行的第一个版本 |

---

## 附：本文档事实核验说明

- 代码事实（死代码、引用关系、diff 范围）均基于 2026-07-04 对工作区的实际检索，非凭印象。
- 法条：上诉期已过元典核验（民诉法 2023 修正第 171 条，现行有效）；保全期限 1/2/3 年出处**未核验**，PRD §13.3 的「民诉法第 244 条」引用存疑，实施 v0.49 前必须逐条核验。
- 「使用率低」的判断依据是导航演进史（v0.11/v0.37/v0.45 的隐藏记录），系统目前没有真实使用埋点——如果叶森的实际使用感受与此不符，以实际感受修正本文档。
